algoritimo exercicio2
inteiro numLista = [], i, maior, x
Inicio
	i = 0
	maior = 0
	x = 0

	enquanto(i < 10){
		escreva("Escreva um número")
		leia(numLista)
		maior = numLista[0]
		se(numLista[i] > maior){
			maior == numLista[i]
		}
		i += 1
	}

	enquanto(x < 10){
		numLista.push = numLista[i] / maior
		x += 1
	}

	escreva(numLista[])
Fim