algoritimo gorjeta
    Real = conta, gorjeta
Inicio
    escreva("Qual o valor da conta?")
    leia(conta)
    gorjeta = (conta * 10) / 100
    escreva("O valor da conta é R$" + conta + " reais e a gorjeta do garçom é R$" + gorjeta + " reais")
Fim 