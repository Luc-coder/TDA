algoritimo exercicio6
inteiro pessoas, i, contadorS, contadorM
caractere sexo, resposta
Inicio
	pessoas = 200
	i = 0
	contadorS = 0
	contadorM = 0

	enquanto(i < pessoas){
		escreva("Qual o seu sexo")
		leia(sexo)
		escreva("Qual sua escolha")
		leia(resposta)

		se(resposta == s){
			contadorS += 1
		}

		se(sexo == m e resposta == n){
			contadorM += 1
		}

		i += 1
	}
Fim