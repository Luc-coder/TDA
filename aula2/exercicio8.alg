algoritimo horas
    Inteiro = hora, minuto, minutagem
Inicio
    escreva("Quantas horas se passaram?")
    leia(hora)
    escrava("Quantos minutos se passaram?")
    leia(minutos)
    minutagem = (hora * 60) + minuto
    escreva(minutagem + "minutos em " + hora + " horas e " + minuto + " minutos")
Fim