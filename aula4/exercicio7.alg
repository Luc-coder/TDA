algoritimo
real alturaEscada, alturaQuadro, distanciaEscada
Inicio
    escreva("Qual a altura da escada?")
    leia(alturaEscada)
    escreva("Qual a altura que o quadro deve ficar?")
    leia(alturaQuadro)
    distanciaEscada = (alturaEscada * 2) / (alturaQuadro * 2)
    escreva("A distancia que a escada deve ficar é de " + distanciaEscada + " cm")
Fim