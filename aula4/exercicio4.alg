algoritimo salario do funcionario
real salarioM, salario, qte
Inicio
    escreva("digite o valor do salário mínimo")
    leia(salarioM)
    escreva("digite o valor do salário do funcionário")
    leia(salario)
    qte = salario / salarioM
    escreva("A quantidade é de R$" + qte)
Fim